import React from 'react';

export default class RespositoryPage extends React.PureComponent {
    public render() {
        return (
            <div>
                repository
            </div>
        );
    }
}